diff --git a/QuoteGeneration/qcnl/linux/network_wrapper.cpp b/QuoteGeneration/qcnl/linux/network_wrapper.cpp
index e0cc945e..7201e2fe 100644
--- a/QuoteGeneration/qcnl/linux/network_wrapper.cpp
+++ b/QuoteGeneration/qcnl/linux/network_wrapper.cpp
@@ -129,6 +129,7 @@ sgx_qcnl_error_t qcnl_https_get(const char* url,
     network_malloc_info_t res_body = {0,0};
 
     do {
+        fprintf(stderr, "[qcnl_https_get] url=%s\n", url);
         curl = curl_easy_init();
         if (!curl)
             break;
@@ -166,6 +167,8 @@ sgx_qcnl_error_t qcnl_https_get(const char* url,
         }
         long http_code = 0;
         curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &http_code);
+        fprintf(stderr, "[qcnl_https_get] http_code=%ld curl_ret=%d body_size=%u header_size=%u\n",
+                http_code, (int)curl_ret, res_body.size, res_header.size);
         if (http_code == 404) {
             ret = SGX_QCNL_ERROR_NO_CERT_DATA;
             break;
diff --git a/QuoteGeneration/qcnl/sgx_default_qcnl_wrapper.cpp b/QuoteGeneration/qcnl/sgx_default_qcnl_wrapper.cpp
index 087f4462..d4ad51c1 100644
--- a/QuoteGeneration/qcnl/sgx_default_qcnl_wrapper.cpp
+++ b/QuoteGeneration/qcnl/sgx_default_qcnl_wrapper.cpp
@@ -39,6 +39,8 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <map>
+#include <algorithm>
+#include <cctype>
 #include <fstream>
 #include <algorithm>
 #include <sgx_key.h>
@@ -187,8 +189,12 @@ static void http_header_to_map(char* resp_header, uint32_t header_size, map<stri
             // parse one line
             string str((unsigned char*)resp_header+start, (unsigned char*)resp_header+end);
             size_t pos = str.find(": ");
-            if (pos != string::npos)
-                header_map.insert(pair<string, string>(str.substr(0, pos), str.substr(pos+2)));
+            if (pos != string::npos) {
+                string key = str.substr(0, pos);
+                transform(key.begin(), key.end(), key.begin(),
+                          [](unsigned char c) { return static_cast<char>(tolower(c)); });
+                header_map.insert(pair<string, string>(key, str.substr(pos+2)));
+            }
             start = end;
         }
     }
@@ -326,14 +332,17 @@ sgx_qcnl_error_t sgx_qcnl_get_pck_cert_chain(const sgx_ql_pck_cert_id_t *p_pck_c
         map<string,string> header_map;
         map<string, string>::const_iterator it;
         http_header_to_map(resp_header, header_size, header_map);
+        fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] parsed_headers=%zu\n", header_map.size());
         it = header_map.find("sgx-tcbm");
         if (it == header_map.end()) {
+            fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] missing sgx-tcbm header\n");
             ret = SGX_QCNL_MSG_ERROR;
             break;
         }
         string tcbm = it->second;
         it = header_map.find("sgx-pck-certificate-issuer-chain");
         if (it == header_map.end()) {
+            fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] missing sgx-pck-certificate-issuer-chain header\n");
             ret = SGX_QCNL_MSG_ERROR;
             break;
         }
@@ -353,6 +362,8 @@ sgx_qcnl_error_t sgx_qcnl_get_pck_cert_chain(const sgx_ql_pck_cert_id_t *p_pck_c
 
         // set tcbm (cpusvn and pcesvn) 
         if (tcbm.size() != (CPUSVN_SIZE + PCESVN_SIZE)*2) {
+            fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] unexpected tcbm size=%zu value=%s\n",
+                    tcbm.size(), tcbm.c_str());
             ret = SGX_QCNL_MSG_ERROR;
             break;
         }
@@ -360,6 +371,8 @@ sgx_qcnl_error_t sgx_qcnl_get_pck_cert_chain(const sgx_ql_pck_cert_id_t *p_pck_c
                                       CPUSVN_SIZE*2, 
                                       reinterpret_cast<uint8_t*>(&(*pp_quote_config)->cert_cpu_svn), 
                                       sizeof(sgx_cpu_svn_t))) {
+            fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] failed to parse cert_cpu_svn from tcbm=%s\n",
+                    tcbm.c_str());
             ret = SGX_QCNL_MSG_ERROR;
             break;
         }
@@ -367,6 +380,8 @@ sgx_qcnl_error_t sgx_qcnl_get_pck_cert_chain(const sgx_ql_pck_cert_id_t *p_pck_c
                                       PCESVN_SIZE*2, 
                                       reinterpret_cast<uint8_t*>(&(*pp_quote_config)->cert_pce_isv_svn), 
                                       sizeof(sgx_isv_svn_t))) {
+            fprintf(stderr, "[sgx_qcnl_get_pck_cert_chain] failed to parse cert_pce_isv_svn from tcbm=%s\n",
+                    tcbm.c_str());
             ret = SGX_QCNL_MSG_ERROR;
             break;
         }
diff --git a/QuoteGeneration/qpl/sgx_default_quote_provider.cpp b/QuoteGeneration/qpl/sgx_default_quote_provider.cpp
index 9c43bf9e..d46d6032 100644
--- a/QuoteGeneration/qpl/sgx_default_quote_provider.cpp
+++ b/QuoteGeneration/qpl/sgx_default_quote_provider.cpp
@@ -36,10 +36,12 @@
 
 #include "sgx_default_quote_provider.h"
 #include "sgx_default_qcnl_wrapper.h"
+#include <cstdio>
 
 quote3_error_t sgx_ql_get_quote_config(const sgx_ql_pck_cert_id_t *p_cert_id, sgx_ql_config_t **pp_quote_config)
 {
     sgx_qcnl_error_t ret = sgx_qcnl_get_pck_cert_chain(p_cert_id, pp_quote_config);
+    fprintf(stderr, "[sgx_ql_get_quote_config] qcnl_ret=0x%04x\n", ret);
     switch (ret){
         case SGX_QCNL_SUCCESS:
             return SGX_QL_SUCCESS;
@@ -58,5 +60,3 @@ quote3_error_t sgx_ql_free_quote_config(sgx_ql_config_t *p_quote_config)
 
     return SGX_QL_SUCCESS;
 }
-
-
